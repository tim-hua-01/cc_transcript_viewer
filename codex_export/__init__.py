"""Exporters that convert other agents' sessions into Codex rollout JSONL.

`cursor_to_codex` and `opencode_to_codex` are standalone CLIs; `codex_rollout`
holds the source-agnostic parts of the rollout format they share.
"""
